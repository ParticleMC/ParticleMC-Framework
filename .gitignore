# Rust 构建产物
/target
target-custom/
debug

# Cargo 注册表缓存
.cargo/registry/
.cargo/git/

# 这些是 rustfmt 生成的备份文件
**/*.rs.bk

# MSVC Windows 构建生成的调试信息
*.pdb

# cargo mutants 生成的数据
**/mutants.out*/

# 开发过程文档（不参与发布）
.specs/

# 独立快照目录（不参与发布）
.standalone/